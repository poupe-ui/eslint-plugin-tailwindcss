import { cssRules } from '../utils/css';

/**
 * Wrapper for \@eslint/css relative-font-units rule
 * Prefers rem/em over px for better accessibility
 */
export const relativeFontUnits = cssRules['relative-font-units'];
